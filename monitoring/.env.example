# Override Grafana admin credentials before exposing the stack beyond localhost.
# Copy this file to `.env` (same directory) and set real values — docker compose
# auto-loads .env on `up`.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change_me_before_exposing
